<Window x:Class="VoiceIn.Ui.HistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="文字起こし履歴"
        Width="640" Height="460"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E2E"
        Foreground="#CDD6F4"
        FontFamily="Segoe UI, Meiryo">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#89B4FA"/>
            <Setter Property="Foreground" Value="#11111B"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#313244"/>
            <Setter Property="Foreground" Value="#CDD6F4"/>
            <Setter Property="BorderBrush" Value="#45475A"/>
            <Setter Property="Padding" Value="6,4"/>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="直近の文字起こし履歴（最新50件）" FontSize="16" FontWeight="Bold" Margin="0,0,0,12"/>

        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="検索:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox x:Name="TxtSearch" Grid.Column="1"
                     VerticalContentAlignment="Center"
                     TextChanged="OnSearchTextChanged"/>
        </Grid>

        <Grid Grid.Row="2">
            <DataGrid x:Name="GridHistory" AutoGenerateColumns="False"
                      Background="#313244" Foreground="#CDD6F4" IsReadOnly="True"
                      SelectionMode="Single">
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsError}" Value="True">
                                <Setter Property="Foreground" Value="#F38BA8"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="日時" Binding="{Binding CreatedAt}" Width="140"/>
                    <DataGridTextColumn Header="プロバイダ" Binding="{Binding Provider}" Width="80"/>
                    <DataGridTextColumn Header="種別" Binding="{Binding Kind}" Width="60"/>
                    <DataGridTextColumn Header="テキスト" Binding="{Binding DisplayText}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
            <TextBlock x:Name="TxtEmpty" Text="該当する履歴がありません"
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       Foreground="#A6ADC8" FontSize="14"
                       Visibility="Collapsed" IsHitTestVisible="False"/>
        </Grid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="選択項目を削除" Click="OnDeleteSelected" Background="#F38BA8" Foreground="#11111B" Margin="0,0,8,0"/>
            <Button Content="全履歴を削除" Click="OnDeleteAll" Background="#F38BA8" Foreground="#11111B" Margin="0,0,8,0"/>
            <Button Content="クリップボードにコピー" Click="OnCopyText" Background="#A6E3A1" Foreground="#11111B" Margin="0,0,8,0"/>
            <Button Content="閉じる" Click="OnClose" Background="#45475A" Foreground="#CDD6F4"/>
        </StackPanel>
    </Grid>
</Window>
